function setup(){
  createCanvas(400,400);
  }

  
function draw()
{
  background('lightgray');
  textSize(35);
  textAlign(CENTER,CENTER);
  fill('blue');
  text('Olá, mundo!',200,200);
}
